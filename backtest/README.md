# backtest/

策略邏輯的 Python 鏡射版本 + 回測引擎。

目的：讓策略邏輯不只活在 Pine Script 裡，而是有一份可以直接餵歷史資料跑的 Python 實作，供未來「agent 自動驗證/回測」使用。這裡的邏輯必須與對應的 `pine/` 策略保持一致（改一邊要記得同步改另一邊）。

尚未實作——等第一個簡單策略定案後，這裡會放：
- 策略邏輯（訊號產生函式，輸入歷史 K 棒，輸出 buy/sell/close）
- 回測引擎（讀歷史資料 → 套用策略邏輯 → 模擬成交 → 產出績效報告）
